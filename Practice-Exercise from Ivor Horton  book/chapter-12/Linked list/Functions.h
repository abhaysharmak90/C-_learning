#ifndef FUNCTIONS
#define FUNCTIONS

#include "truckload.h"

void Create_Load(std::vector<truckload *> &);

void Display_Load(std::vector<truckload *> &);

void Add_Items(std::vector<truckload *> &);

void Remove_Items(std::vector<truckload *> &);

#endif